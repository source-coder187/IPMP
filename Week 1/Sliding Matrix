#include <bits/stdc++.h>
using namespace std;
 
int main() {
	int t;
	cin >> t;
	while(t--){
	    long long n,m,r,c;
	    cin >> n >> m >> r >> c;
	    long long sum = n*m-(r-1)*m - c + (n-r)*(m-1);
	    cout << sum << endl;
	}
 
}
